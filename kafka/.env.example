# Kafka KRaft Configuration
KAFKA_NODE_ID=1
KAFKA_PORT=9092
KAFKA_CONTROLLER_PORT=9093

# Network Configuration
# Set to your LAN IP for access from other devices on local network
# Examples: 192.168.1.100, 10.0.0.50
# Leave as 'localhost' for local-only access
YOUR_LAN_IP=localhost

# Kafka Cluster ID (generate with: kafka-storage random-uuid)
# This is a sample UUID - generate your own for production
KAFKA_CLUSTER_ID=MkU3OEVBNTcwNTJENDM2Qk

# Kafka Topic Configuration
KAFKA_REPLICATION_FACTOR=1
KAFKA_MIN_ISR=1
KAFKA_NUM_PARTITIONS=3
KAFKA_AUTO_CREATE_TOPICS=true
KAFKA_LOG_RETENTION_HOURS=168
KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS=0

# Kafka UI Configuration
KAFKA_UI_PORT=9000
KAFKA_CLUSTER_NAME=local
